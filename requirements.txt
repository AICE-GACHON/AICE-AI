# 수집 단계 (1주차)
requests>=2.32
python-dotenv>=1.0
pyjwt>=2.8          # 캐시된 로그인 토큰 만료 검사
pytest>=8.0         # 테스트

# 임베딩 (2주차) — Python 3.14에서 정상 설치 확인됨
# CPU 빌드로 충분하다 (43,515편 전체 임베딩에 CPU로 약 0.8시간, GPU 불필요).
# CPU 휠 설치: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.9
transformers>=4.57
adapters>=1.3       # SPECTER2는 base 모델 + adapter 구조

# 이후 단계에서 추가 예정:
# langgraph, anthropic      — 쿼리 파이프라인 (3주차)
# psycopg[binary], pgvector — DB 적재 (2주차)
